public class Main {
    public static void main(String[] args) {
        Fila fila = new Fila(6);
        fila.insere(1);
        fila.insere(2);
        fila.insere(3);
        fila.insere(4);
        fila.remove();
        fila.insere(6);
        fila.insere(3);
        fila.insere(5);
        fila.remove();
        fila.insere(10);
        fila.remove();
        fila.remove();
        fila.remove();
        fila.remove();
        fila.remove();
        fila.remove();
        fila.insere(11);
        fila.insere(12);
        fila.remove();
        fila.remove();
        fila.insere(10);
        fila.insere(12);
        fila.remove();
        fila.remove();
        fila.remove();
        fila.insere(2);
        fila.insere(4);
        fila.insere(5);
        fila.insere(78);
        fila.remove();
        fila.remove();
        fila.remove();
        fila.remove();

    }
}